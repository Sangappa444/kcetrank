{
  "name": "antigravityproject-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix kcetmain/kcet/backend start\" \"npm --prefix frontend run dev\"",
    "build": "cd frontend && npm run build",
    "start": "npm run dev",
    "vercel-build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
